val month = "(JAN|FEB|MAR|APR|MAY|JUN|JUL|AUG|SEP|OCT|NOV|DEC)"

fun getPattern(): String = """\d{2} $month \d{4}"""

//Fungsi `getPattern` mengembalikan pola regex untuk mencocokkan format tanggal dengan format "dd MMM yyyy" di mana MMM adalah salah satu dari JAN, FEB, ..., DEC.
